{
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.3",
    "globals": "^17.4.0",
    "jest": "^30.2.0"
  },
  "scripts": {
    "test": "jest",
    "lint": "eslint ."
  },
  "jest": {
    "collectCoverage": true,
    "coverageThreshold": {
      "global": {
        "coverage": "jest --coverage"
      }
    }
  }
}
